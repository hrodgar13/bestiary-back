export enum IMAGE_POSITIONS {
    right = 'right',
    left = 'left'
}

export enum STRUCTURE_PARAGRAPH_TYPES {
    text = 'text',
    number = 'number',
    date = 'date',
    image = 'image',
    list = 'list'
}
